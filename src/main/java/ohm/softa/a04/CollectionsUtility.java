package ohm.softa.a04;

import java.util.Comparator;

public abstract class CollectionsUtility {
    /*public <T> SimpleList<T> sort(SimpleList<T> list, Comparator<T> comparator) throws InstantiationException, IllegalAccessException {
        SimpleList<T> copy = list.copy();
        SimpleList<T> result = list.getClass().newInstance();
        while(result.size() != list.size()) {
            T min = copy.getItem(0);
            for(int i = 0; i < copy.size(); i++) {
                if(comparator.compare(min, copy.getItem(i)))
            }
        }
        return result;
    }*/
}
